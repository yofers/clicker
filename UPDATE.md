# 更新日志 (2026-01-14)

## 新增功能与优化

### 1. 全新功能：动作录制器 (Recorder)
新增了强大的动作录制与回放功能，支持用户录制复杂的鼠标与键盘操作序列并重复执行。
- **动作录制**: 
  - 支持记录鼠标移动、点击以及键盘按键操作。
  - 支持自定义保存文件名（自动生成 `.yfc` 格式文件）。
  - 录制过程中提供实时状态反馈。
- **动作回放**: 
  - 支持选择已录制的文件进行回放。
  - **回放设置**: 支持自定义回放间隔（毫秒）和循环次数（0 为无限循环）。
  - **文件管理**: 提供下拉列表选择录制文件，支持删除文件和导入外部文件。
- **模式切换**: 通过主界面左上角按钮或 `Ctrl+F6` 可在“连点器”、“长按器”和“录制器”之间无缝切换。

### 2. 快捷键重构 (Shortcuts)
针对不同模式重新定义了快捷键行为，使其更符合直觉：
- **Ctrl+F7**: 
  - **连点器/长按器模式**: 启动/停止 **鼠标** 任务。
  - **录制器模式**: 开始/停止 **动作录制**。
- **Ctrl+F8**: 
  - **连点器/长按器模式**: 启动/停止 **键盘** 任务。
  - **录制器模式**: 开始/停止 **动作回放**。
- **动态帮助**: 左下角的快捷键提示现在会根据当前所处模式动态更新显示内容。

### 3. 交互逻辑保护 (Logic Safety)
为了防止误操作导致的状态混乱，增加了以下限制：
- **录制锁定**: 当正在录制动作时：
  - 禁止通过按钮或快捷键 (Ctrl+F6) 切换应用主模式（连点器/长按器）。
  - 禁止切换到“执行动作”标签页。
  - 相关切换按钮会自动变为禁用状态，并提供文字提示。

### 4. 界面修复 (UI Fixes)
- **下拉框布局溢出修复**: 
  - 解决了在“执行动作”页面中，如果选中的录制文件名过长，会导致下拉框宽度异常撑开，进而挤压右侧“导入文件”按钮的问题。
  - 现在过长的文件名会自动截断并显示省略号 (...)，保持界面布局整洁。
